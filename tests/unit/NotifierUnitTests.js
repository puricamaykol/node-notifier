var expect = require('chai').expect


describe('Notifier', function() { 

	describe('# Send()', function() { 
		it("Should accept only well formed value objects", function(){

		});

		it("Should send message through every channel especofied in value object", function(){

		});
		it("Should return an Array of objects with response info for each channel used", function(){

		});

		it("Should not send message in a channel with missing payload info", function() {});

		it("Should not send message without value object", function() {});

		it("Should return propper error message for a channel with malformed payload object", function() {});

		it("Should return propper error message for a channel that doesn't exist", function() {});

	});

});